// Compatibility export for references outside the router. The production route
// imports the clean-room feature module directly.
export { CheckoutWorkspace as PaymentPage } from '../features/checkout/CheckoutWorkspace'
