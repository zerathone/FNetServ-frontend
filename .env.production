# Build PROD — GHI ĐÈ bằng `.env.local` với IP LAN thật của server (không commit .env.local).
# Nếu quên set, client.ts fallback về http://127.0.0.1:18099 (SAI cho tablet — phải override).
VITE_API_BASE_URL=http://192.168.1.10:18099
